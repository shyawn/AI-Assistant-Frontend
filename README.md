# AI-Assistant Frontend (React Vite)

This repository is used for serving the frontend component of the AI Assistant project.

### Install Dependencies in Repo

```bash
$ yarn install
```

### Running Local Setup

```bash
$ npm install -g @ionic/cli
$ npm run dev
```
